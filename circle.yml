dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install openssl libapr1
      
machine:
  java:
    version: oraclejdk8

deployment:
  deploysnap:
    branch: 5.1.1
    commands:
      - mvn deploy -DskipTests -s settings.xml


